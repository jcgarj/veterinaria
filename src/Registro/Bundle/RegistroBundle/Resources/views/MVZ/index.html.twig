{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MVZ list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Appat</th>
                <th>Apmat</th>
                <th>Calle</th>
                <th>Noext</th>
                <th>Noint</th>
                <th>Colonia</th>
                <th>Delegacion</th>
                <th>Telefono</th>
                <th>Correo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('mvz_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.appat }}</td>
                <td>{{ entity.apmat }}</td>
                <td>{{ entity.calle }}</td>
                <td>{{ entity.noExt }}</td>
                <td>{{ entity.noInt }}</td>
                <td>{{ entity.colonia }}</td>
                <td>{{ entity.delegacion }}</td>
                <td>{{ entity.telefono }}</td>
                <td>{{ entity.correo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('mvz_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mvz_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('mvz_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
